function salutation(salute: string, name: string): string {
    return `Hello ${salute} ${name}, how are you?`
}

console.log(salutation('Mrs.', 'Noêmia')); // Hello Mrs. Noêmia, how are you?
console.log(salutation('Mano', 'Brown')); // Hello Mano Brown, how are you?