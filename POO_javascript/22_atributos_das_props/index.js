let person = {
    name: "Adriane"
}

// person -> Object

console.log(Object.getOwnPropertyDescriptor(person, 'name'));
// console.log(Object.getOwnPropertyDescriptor(window, 'scrollX'));

// Verificar abributos default dos objetos